function scrollToTop(topScroll) {
	if (typeof topScroll == "number") {
		return topScroll;
	} else {
		return "ERROR!";
	}
}

function scrollBehaviour(behaviourScroll) {
	if (typeof behaviourScroll == "string") {
		return behaviourScroll;
	} else {
		return "ERROR!";
	}
}

function windowsScrollTop(windowsTopScroll) {
	let windowsScrollTop = 500;
	if (windowsTopScroll > 300) {
		return windowsTopScroll;
	} else {
		return "ERROR!";
	}
}

function stripePublicKeySlices(slices) {
	if (typeof slices == "number") {
		return slices;
	} else {
		return "ERROR!";
	}
}

function baseColor(color) {
	if (typeof color == "string") {
		return color;
	} else {
		return "ERROR!";
	}
}

function baseTextTransform(textTransform) {
	if (typeof textTransform == "string") {
		return textTransform;
	} else {
		return "ERROR!";
	}
}

function baseFontFamily(fontFamily) {
	if (typeof fontFamily == "string") {
		return fontFamily;
	} else {
		return "ERROR!";
	}
}

function baseFontSmoothing(fontSmoothing) {
	if (typeof fontSmoothing == "string") {
		return fontSmoothing;
	} else {
		return "ERROR!";
	}
}

function baseFontSize(fontSize) {
	if (typeof fontSize == "string") {
		return fontSize;
	} else {
		return "ERROR!";
	}
}

function InvalidColor(colorAndIconColor) {
	if (typeof colorAndIconColor == "string") {
		return colorAndIconColor;
	} else {
		return "ERROR!";
	}
}

function formDisabled(disabled) {
	if (typeof disabled == "boolean") {
		return disabled;
	} else {
		return "ERROR!";
	}
}

function formFadeToggle(fadeToggle) {
	if (typeof fadeToggle == "number") {
		return fadeToggle;
	} else {
		return "ERROR!";
	}
}

function postData(postDataContents) {
	if (typeof postDataContents == "string") {
		return postDataContents;
	} else {
		return "ERROR!";
	}
}

function url(urlLink) {
	if (typeof urlLink == "string") {
		return urlLink;
	} else {
		return "ERROR!";
	}
}

function paymentIntentStatus(paymentIntent) {
	if (typeof paymentIntent == "string") {
		return paymentIntent;
	} else {
		return "ERROR!";
	}
}